{
  "name": "@qbit/auth-common",
  "version": "0.1.0",
  "description": "Shared authentication utilities for QBit Accounting System",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && npx tsc -p tsconfig.json && echo \"✅ @qbit/auth-common build completed successfully\"",
    "clean": "rimraf dist",
    "dev": "tsc --watch",
    "lint": "eslint --ext .ts src",
    "test": "jest",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "auth",
    "qbit",
    "accounting",
    "authentication"
  ],
  "author": "QBit Accounting Team",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@types/jsonwebtoken": "^9.0.0",
    "@types/passport-jwt": "^3.0.9",
    "jsonwebtoken": "^9.0.0",
    "passport": "^0.6.0",
    "passport-jwt": "^4.0.1"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/passport": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.3"
  }
}
