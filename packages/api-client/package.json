{
  "name": "@qbit/api-client",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc && echo \"✅ @qbit/api-client build completed successfully\"",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "lint": "eslint \"src/**/*.ts*\"",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@qbit/shared-types": "*",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prisma": "^6.6.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
